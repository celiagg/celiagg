language: generic

matrix:
  include:
    - sudo: required
      services:
        - docker
      env:
        - BUILD_TYPE="package"
        - DOCKER_IMAGE=quay.io/pypa/manylinux1_x86_64

install:
  - docker pull $DOCKER_IMAGE
script:
  - ./travis/build-run.sh
